# 랭킹 구현 과제

아래 요건에 맞게 랭킹 화면을 Vue 3로 구현하세요.

---

## 기능 요건

### 데이터
- 아이템은 `id`와 `rank`를 가진 8개의 객체 배열로 구성
- 화면에는 `rank` 기준으로 오름차순 정렬된 순서로 표시

### 랭킹 레이아웃
- 8개의 아이템을 CSS Grid로 배치
- 1~3위는 시상대(포디움) 형태로 배치
  - 1위: 가운데, 가장 높은 높이
  - 2위: 왼쪽
  - 3위: 오른쪽
- 4~8위는 전체 너비를 차지하며 순서대로 아래에 나열
- 각 아이템은 순위에 따라 배경색과 크기가 다르게 표시됨

---

## 유의사항

- JavaScript의 배열 `sort()`는 원본 배열을 직접 변경합니다.
- 순위에 따라 Grid 내 위치와 크기를 다르게 지정해야 합니다.
- 동적 클래스 바인딩을 활용해 순위별 스타일을 분기할 수 있습니다.
