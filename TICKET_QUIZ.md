# 티켓 구현 과제

`pages/index.vue`에 아래 요건에 맞게 쿠폰 티켓 화면을 Vue 3로 구현하세요.
완성 코드는 `pages/compelete/index.vue`를 참고하세요.

---

## 기능 요건

### 데이터
- 티켓은 아래 속성을 가진 4개의 객체 배열로 구성
  - `type`: 방향(`horizontal` / `vertical`)과 모서리 스타일(`straight` / `round`) 두 값을 가진 배열
  - `price`, `unit`, `name`, `term`, `period`: 노출 텍스트
  - `down`: 다운로드 여부 (초기값 `false`)

### 티켓 구조
- 각 티켓은 **정보 영역**과 **버튼 영역** 두 파트로 구성
- `horizontal` (가로형): 정보 영역이 넓고 버튼 영역이 우측에 세로로 붙음. 텍스트(이용약관·유효기간)는 정보 영역에 표시
- `vertical` (세로형): 정보 영역이 위, 버튼 영역이 아래에 붙음. 텍스트(이용약관·유효기간)는 버튼 영역에 표시
- 티켓에 드롭섀도우 효과 적용

### 모서리 스타일
- `straight`: 각진 모서리 — `clip-path: polygon()`으로 처리
- `round`: 둥근 모서리 — DOM 크기를 기반으로 계산한 SVG path로 처리
  - 티켓 정보 영역과 버튼 영역 각각 path 계산 필요
  - 정보 영역은 테두리 효과를 위한 내부 path도 별도 계산
  - 창 크기가 바뀔 때마다 재계산

### 다운로드
- 티켓 클릭 시 `down`이 `true`로 변경
- 버튼 영역의 텍스트와 아이콘, 배경색이 변경됨

---

## 유의사항

- `drop-shadow`와 `clip-path`를 함께 사용할 때 적용 위치에 주의하세요.
- `v-for` 안에서 `ref`를 사용하면 단일 값이 아닌 배열로 수집됩니다.
- DOM 크기를 기반으로 계산하는 경우, 컴포넌트 상태가 바뀔 때마다 재계산이 필요한 시점을 고려하세요.
- resize 이벤트를 등록했다면 컴포넌트가 사라질 때 반드시 해제해야 합니다.
- CSS `<style scoped>` 안에서 Vue 반응형 값을 활용할 수 있습니다.
